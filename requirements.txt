# Embedding Inference Benchmark - Python Dependencies
#
# Install with: pip install -r requirements.txt

# Google Cloud Vertex AI SDK for model deployment and endpoint management
google-cloud-aiplatform>=1.60.0

# Async HTTP client for high-concurrency load generation
aiohttp>=3.9.0

# YAML parsing for configuration files
pyyaml>=6.0

# NumPy for statistical calculations (percentiles, etc.)
numpy>=1.24.0
