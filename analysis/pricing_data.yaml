# =============================================================================
# GCP GPU Instance Pricing for Cost Analysis
# =============================================================================
#
# These prices are used to calculate the $/million tokens metric in the
# throughput-vs-cost matrix.
#
# Pricing source: https://cloud.google.com/compute/gpus-pricing
# Last updated: May 2026
#
# How cost calculation works:
#   1. We measure throughput (tokens/sec) at each concurrency level
#   2. We calculate tokens/hour = tokens/sec × 3600
#   3. We calculate $/million_tokens = (hourly_cost / tokens_per_hour) × 1,000,000
#
# This gives the user the key metric they need: for every million tokens
# they process, how many dollars does it cost in infrastructure?
# =============================================================================

# ---------------------------------------------------------------------------
# G4 Instances (NVIDIA RTX Pro 6000) — What we're benchmarking
# ---------------------------------------------------------------------------
# RTX Pro 6000 has 96GB VRAM, higher memory bandwidth, and more CUDA cores.
# On-demand GPU price: $4.50/hr per GPU.
# ---------------------------------------------------------------------------
g4:
  gpu_name: "NVIDIA RTX Pro 6000"
  gpu_memory_gb: 96

  # g4-standard-48: 48 vCPU, 192 GB RAM, 1× RTX Pro 6000
  single_gpu:
    machine_type: "g4-standard-48"
    accelerator_count: 1
    on_demand_hourly_usd: 4.50
    cud_1yr_hourly_usd: 2.835      # ~37% discount
    cud_3yr_hourly_usd: 2.025      # ~55% discount

  # 2-node config: 2× g4-standard-48 each with 1× RTX Pro 6000
  # Architecture: 2 separate Vertex AI replicas (nodes), load-balanced.
  # Each node has 1 GPU. This is NOT tensor-parallel across 2 GPUs on 1 node.
  # Pricing is 2× the single_gpu cost (2 independent VMs).
  dual_gpu:
    machine_type: "g4-standard-48"
    accelerator_count: 1          # 1 GPU per node
    num_nodes: 2                  # 2 separate Vertex AI replicas
    # Total hourly cost = 2 × single-node cost
    on_demand_hourly_usd: 9.00    # 2 × 4.50
    cud_1yr_hourly_usd: 5.67      # 2 × 2.835
    cud_3yr_hourly_usd: 4.05      # 2 × 2.025
